if [[ $SHLVL != 1 ]]; then

	printf "\n$(tput smul;tput setaf 3)\xE2\x9C\x8B  wax doesn't like running in a subshell!$(tput sgr0)\n"
	printf "   please try again using \"wax <instruction> <module>\"\n\n"
	exit 1

fi
